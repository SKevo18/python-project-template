# TODO: Your editor's folder
# .vscode/

# After you uncomment the line above/ignore your editor's folder, you can
# run `git rm -r --cached .vscode` from your workspace root, so the folder becomes
# untracked/ignored by git (committing will mark the folder as deleted)

__pycache__/
.pytest-cache/
# generated by pytest
env/
# `python -m venv env` from project root

documentation/
# will be generated by GH Actions immediately, just setup GH Pages in your repo settings

# For PyPI:
dist/*
*.egg-info
